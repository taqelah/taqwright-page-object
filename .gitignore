# Node
node_modules/
npm-debug.log*

# Logs (e.g. per-device Appium server logs: appium-5554.log)
*.log

# Downloaded app binaries
*.apk
*.app
*.ipa
*.app.zip
# …except the demo-app binaries committed under app/ (self-contained project)
!app/DemoApp-v1.0.0.apk
!app/DemoApp-v1.1.0-debug-ios.app.zip

# Generated test reports / artifacts
test-results/
playwright-report/
taqwright-report/
report.html
fail-*.png

# Secrets / local env (keep .env.example, ignore real .env)
.env

# Claude Code — share .claude/, keep personal overrides local
.claude/settings.local.json

# OS / editor
.DS_Store
